# k8s/databases/auth-database-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-database
  namespace: auth
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-database
  template:
    metadata:
      labels:
        app: auth-database
    spec:
      containers:
      - name: auth-database
        image: mysql:latest
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_DATABASE
          value: "${DB_NAME}"
        - name: MYSQL_USER
          valueFrom:
            secretKeyRef:
              name: auth-db-credentials
              key: username
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: auth-db-credentials
              key: password
        - name: MYSQL_ROOT_PASSWORD
          value: "root"
